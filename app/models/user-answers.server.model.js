'use strict';

/**
 * Module dependencies.
 */
var mongoose = require('mongoose'),
	Schema = mongoose.Schema;

/**
 * UserAnswers Schema
 */
var UserAnswersSchema = new Schema({
	// UserAnswers model fields   
	// ...
});

mongoose.model('UserAnswers', UserAnswersSchema);